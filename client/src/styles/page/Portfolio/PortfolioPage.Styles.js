import styled from 'styled-components';
import {
	maxView,
	minView,
	miscMinView,
} from '../../globalstyles/mediaQueries.styles';

export const PortfolioPageContainer = styled.main`
	margin-top: 10em;
`;
